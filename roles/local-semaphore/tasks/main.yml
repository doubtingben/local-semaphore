---

- name: ensure volumes exist
  file:
    path: "{{ item }}"
    state: directory
  with_items:
  - "{{ local_semaphore.playbook_path }}"

- name: template config files
  template:
    src: "{{ item.src }}.j2"
    dest: "{{ item.dest }}/{{ item.src }}"
  with_items:
    - { src: 'env', dest: "{{ local_semaphore.playbook_path }}" }


