---

- hosts: all
  remote_user: root
  gather_facts: no
  pre_tasks:
    - name: 'Install python'
      raw: sudo apt-get -y install python-simplejson

  tasks:

  - name: Hostname
    command: hostname

...
